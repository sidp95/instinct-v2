import { type AxiosInstance } from 'axios';
import { AuthMode } from '../types.js';
import { ForwardMPCClient } from '@dynamic-labs-wallet/forward-mpc-client';
export declare class BaseClient {
    apiClient: AxiosInstance;
    baseApiUrl: string;
    environmentId: string;
    clientKeysharesRelayBaseApiUrl: string;
    clientRelayApiClient: AxiosInstance;
    forwardMPCClient: ForwardMPCClient;
    protected authMode: AuthMode;
    protected backupServiceAuthToken?: string;
    constructor({ environmentId, baseApiUrl, authToken, backupServiceAuthToken, baseClientKeysharesRelayApiUrl, authMode, sdkVersion, forwardMPCClient, }: {
        environmentId: string;
        baseApiUrl?: string;
        authToken?: string;
        backupServiceAuthToken?: string;
        baseClientKeysharesRelayApiUrl?: string;
        authMode?: AuthMode;
        sdkVersion?: string;
        forwardMPCClient?: ForwardMPCClient;
    });
    syncAuthToken(authToken: string): void;
    syncBackupServiceAuthToken(backupServiceAuthToken: string): void;
}
//# sourceMappingURL=client.d.ts.map