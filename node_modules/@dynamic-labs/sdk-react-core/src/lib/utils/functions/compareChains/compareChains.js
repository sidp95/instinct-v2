'use client'
const synonyms = {
    bip122: 'BTC',
    bitcoin: 'BTC',
    eip155: 'EVM',
    solana: 'SOL',
    sui: 'SUI',
    ton: 'TON',
};
for (const chain in synonyms)
    synonyms[synonyms[chain]] = chain;
const compareChains = (chain1, chain2) => chain1 === chain2 ||
    synonyms[chain1] === chain2 ||
    synonyms[chain2] === chain1;

export { compareChains };
